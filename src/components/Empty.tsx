export const Empty = () => {
  /** WIP @todo -- replace */
  return (
    <div className="flex flex-col items-center justify-center h-full">
      <div className="animate-spin rounded-full p-2 border-t-2 border-b-2 border-gray-900">{`It's empty😵`}</div>
    </div>
  );
};
